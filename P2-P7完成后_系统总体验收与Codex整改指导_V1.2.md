# P2—P7 完成后：系统总体验收与 Codex 整改指导 V1.2

> 审阅日期：2026-09-22。审阅基线：`88eeb967f5d0441b6d4d71c4561ab67eba577d47`，提交说明 `09223`。
>
> 仓库：<https://github.com/lizhao013216-prog/peixun>。源码根目录为 `source/`。
>
> 本次交付是审阅结论及代码整改指导，没有修改、提交或推送项目代码。文中新增组件、接口及验收项均为后续实施要求。

## 0. 给项目负责人的结论

P2—P7 已经建立了独立工程、模板快照、可变步骤、配置驱动训练、保障教学副本和问题分流等重要基础，应保留这些成果。当前还不能按“面向教员、学员的完整演示已验收”收尾。

主要差距有三类：

1. **可视化没有接上新数据模型。** 新场景编辑器显示位置图标，新课件和训练显示对象卡片，原设备示意图留在旧组件中。用户指出的“完整设备场景图看不到”有明确代码原因。
2. **部分制作配置没有被运行正确消费。** 课件逐步预览每次重新初始化；保障课件可以自由编辑步骤，但运行仍固定六阶段；计算后再次改方案也能提交过期结果。
3. **新旧入口和对象契约没有全部统一。** 记录页问题登记参数不完整，旧转任务入口绕过新分流规则，新保障任务字段与原预案不匹配，新台位接口未接入现有台位页面。

本轮应做增量整改，保留当前整体配色、字体、三系统导航、Java/Vue/H2技术栈及已经正确运行的历史流程。

### 0.1 审阅证据的边界

| 证据类型 | 本次实际完成 |
| --- | --- |
| 最新提交 | 通过GitHub核对main分支，按上述SHA获取源文件，未用旧本地检出代替最新代码 |
| 静态审阅 | 前后端关键业务实现、路由、数据投影、迁移、阶段报告和验证脚本 |
| 前端构建 | 最新前端 `npm run build` 通过，含类型检查 |
| 后端复现 | 最新Java源文件编译后，用与项目Spring Boot版本匹配的已有依赖、独立H2内存库运行，执行针对性HTTP验证 |
| 浏览器复验 | 本环境缺少可启动的Chromium内核，未完成浏览器交互/截图复验；UI判断依据当前组件和样式代码，不能写成已通过视觉验收 |
| 未重新验证 | 仓库记载的48项JUnit全量测试、全部既有浏览器脚本、用户实际数据库迁移、仓库当前预编译运行包一致性 |

本文用“已复现”表示实际HTTP执行结果，用“代码确认”表示当前实现可直接确认的问题。代码确认的页面问题仍须由后续Codex在浏览器中复验。

### 0.2 第一次交给Codex的提示词

```text
请完整阅读《P2-P7完成后_系统总体验收与Codex整改指导_V1.2.md》，核对当前仓库、AGENTS.md及source/docs/业务重构实施进度.md。

以当前最新代码继续，不回退到88eeb96，不覆盖我后续修改，不重做P0—P7。
本次只执行R1“恢复统一设备场景视图”，重点落实第2节和R1验收项。

要求：使用真实工程/模板/课件/任务快照中的设备、布局与状态，恢复清晰专业的完整设备场景；不能把旧固定四设备图贴到所有新课件，也不能只保留一排Box图标。
覆盖场景编排、工程调试、模板查看、课件预览、培训任务详情、训练运行及结果查看。保留当前UI风格。
不建设真实VR引擎，使用数据驱动的二维或2.5D示意组件。

完成实际代码、必要的接口适配及浏览器验证，给出入口、操作步骤、设备图截图和实际测试结果。更新实施记录，说明未完成项，交付一个可运行增量后停止。
不要直接连续执行R2—R6，不要只输出分析，不要自动推送。不得清空或重置用户数据库。
如发现本轮修复依赖后续问题，只完成必要前置并解释，不静默扩展成整轮重构。
```

后续提示词：

```text
继续依据V1.2整改指导，执行我指定的下一批R编号。先核验已完成代码和验收证据，避免重复开发；本次只完成这一批。
交付真实可运行修改、相关回归及Markdown实施记录；完成后停止并给出我能直接操作的验收路线。
```

## 1. 问题清单与优先级

优先级含义：**阻断**为业务操作失败、证据/计算结果错误或绕过业务规则；**高**为核心使用体验、制作能力或配置与运行不一致；**中**为业务组织和验收完善。它们与上一轮P0—P7阶段编号无关。

| 编号 | 优先级 | 结论 | 证据 | 整改批次 |
| --- | --- | --- | --- | --- |
| F01 | 高 | 完整设备场景被图标/卡片替代，多页面没有统一场景视图 | 代码确认，用户反馈一致 | R1 |
| F02 | 高 | 联动草稿切换页签会丢失；编辑版本未稳定绑定本地草稿 | 代码确认，浏览器待复验 | R2 |
| F03 | 高 | 课件预览逐步重置，不使用用户实际输入；与正式训练不一致 | 连续预览问题已复现，参数路径代码确认 | R2 |
| F04 | 高 | 保障教学固定案例和六阶段，未按课件步骤/评分执行 | 已复现 | R3 |
| F05 | 阻断 | 保障方案修改后可提交旧计算结果和已取得分数 | 已复现 | R3 |
| F06 | 阻断 | 训练记录页“记录问题”缺少type，保存报错 | 已复现 | R4 |
| F07 | 阻断 | 旧task.create仍可从未核实的内容问题生成保障任务 | 已复现 | R4 |
| F08 | 阻断 | 新转保障任务使用dueMinutes/type，原预案读取deadline/taskType | 已复现，预案deadline为null | R4 |
| F09 | 阻断/高 | 台位前后端脱节、领域列表未隔离，结束实例接收信号返回500 | 接口与投影已复现，入口代码确认 | R5；500防护宜提前 |
| F10 | 阻断/高 | 重复确认生成重复归档；归档菜单实际主要展示训练记录JSON | 重复归档已复现，页面代码确认 | R4 |
| F11 | 高 | 素材行为靠componentId推断；联动页不能完整编辑通用规则 | 代码确认 | R5 |
| F12 | 中 | 课程组织及任务组织缺少完整界面入口 | 代码确认 | R5 |
| F13 | 高 | 晚到响应未核对领域/身份，部分权限硬编码教员账号ID | 代码确认，并发场景待复验 | R2 |
| F14 | 高 | 步骤模式、完成条件与运行口径不完全一致，自由训练仍可自动填答案 | 代码确认 | R2 |
| F15 | 高 | 综合验收主要覆盖API成功路径，没有证明普通用户可从页面完成全部流程 | 脚本确认 | R6 |

阻断项未修复前，实施记录应标明限制，不继续笼统写“业务闭环全部验收通过”。先交付R1让设备场景恢复可见，再按批次修正行为；不要把阻断项归为将来正式系统再处理。

## 2. R1：恢复统一设备场景视图

### 2.1 问题根因与代码位置

| 位置 | 当前实现 | 后果 |
| --- | --- | --- |
| `source/frontend/src/components/SceneView.vue` | 固定绘制PUMP-01、VALVE-01、CTRL-01、SENSOR-01，标题固定“维修工位” | 仍有旧完整示意图，但不能表达任意新工程 |
| `components/LessonStep.vue` | `objects?.length`成立时使用`.courseware-object-canvas`卡片网格，否则才显示SceneView | 新V3课件主动绕过完整设备图；对象位置和联动不展示 |
| `views/SimulationProjectEditor.vue` | `.scene-plane`上显示统一Box图标和名称，按x/y百分比定位 | 有布局数据但缺少设备形态、场景环境和关系表达 |
| `components/SimulationPreviewPanel.vue` | 操作表单、状态JSON卡片、事件列表 | 调试时看不到场景中的变化 |
| `views/SimulationTemplatesView.vue` | 设备组成和规则文字列表 | 选模板时难以判断它对应什么训练环境 |
| `components/TrainingAssignments.vue` | 任务卡片和开始确认弹窗均无场景预览 | 学员开始前不知道将操作哪套设备 |
| `components/TrainingPlayer.vue` | 把快照objects与runtime.states交给LessonStep | 已有正确的数据来源，主要缺统一渲染层 |

这是组件接入和数据表达问题，不能只修改CSS尺寸，也不能只恢复一张固定泵组图片。

### 2.2 统一组件及边界

建议新增 `components/EquipmentSceneView.vue`，把现有SceneView中的设备形态拆成可复用的SVG设备符号；旧组件作为兼容适配器保留。允许采用等价组织，避免两个互不相干的新渲染器。

职责：显示设备形态、位置、名称、状态、当前选中对象、当前步骤目标和可选联动；发出对象选择或布局修改意图。业务动作、计分、权限仍由原命令和后端服务执行。

推荐输入契约：

```ts
type EquipmentSceneInput = {
  contextId: string;
  domain: 'OPERATION' | 'MAINTENANCE' | 'SUPPORT';
  scene: SceneSnapshot;
  topology?: TopologySnapshot;
  states?: Record<string, Record<string, unknown>>;
  selectedObjectId?: string;
  targetObjectId?: string;
  mode: 'edit' | 'preview' | 'training' | 'result';
  readOnly: boolean;
  showRelations: boolean;
};
```

该类型是推荐设计，不是当前已有代码。输出至少有 `selectObject(id)`；编辑模式可有 `moveObject(id, position)`，必须通过工程草稿保存，不直接改发布快照。

### 2.3 完整设备场景的最低标准

- 显示本场景全部设备，具有可区分的形态：泵、电机/控制柜、阀门、传感器、工具、工作台等可用清晰SVG符号。
- 保留统一场景底图或工位环境、设备名称、位置关系和图例。未知设备使用有名称和类别的通用设备图形，不能假装渲染成泵。
- 第五个及更多实例按数据渲染；同类多个设备必须独立选择和变更状态。
- 支持总览、适应全部、缩放和放大查看；移动端可打开完整场景视图，不能简单隐藏场景。
- 工程采用当前x/y的0—100布局时，转换为统一坐标系；适应视口时计算设备及标签边界，x/y靠近边缘也不能被`overflow:hidden`截掉。
- 联动连接作为可开关的覆盖层，标注信号方向及含义；不能把逻辑信号线误画成真实管路。
- 高亮当前选中对象；引导模式可标目标，自由模式不直接泄露目标。状态颜色同时配合中文标签，显示就绪、运行、停止、告警等实际状态。
- 训练动作成功后，场景状态和结果提示来自同一次服务端响应；拒绝动作不能先在图上显示成功。
- 标题来自当前系统和场景名称，不能三个系统都显示“泵组维修工位”。
- 保留用户已认可的整体配色和字体。优先调整内容层级及画布占比，不重新做一套全站皮肤。

“完整”指当前业务场景的完整对象和关系，不要求本轮解析真实FBX/STEP或实现物理仿真。

### 2.4 视觉元数据

可以增加有限的视觉描述，例如 `visual.kind`、`visual.symbolKey`、宽高、旋转和标签位置；视觉描述与设备动作能力分开。优先复用已有素材类别、组件标识和对象坐标。

推荐在模拟资源中配置明确的符号类型，不使用设备中文名称猜图形。发布时将必要视觉描述冻结到场景/课件快照中；历史模板再次打开时应保持相同视觉配置。

没有视觉描述的旧对象可经只读适配推导默认符号，必要时做增量迁移。不能因补图而重写旧训练事件和分数，不能为兼容重新让新页面读全局scene/topology。

### 2.5 每个页面应使用哪份数据

| 页面 | 场景与关系来源 | 状态来源 | 展示方式 |
| --- | --- | --- | --- |
| 编排编辑 | 当前工程本地草稿；已保存版本为基线 | 初始状态 | 可选择、布局和查看属性 |
| 工程调试 | 调试实例冻结的工程配置 | 当前preview.states | 操作和事件旁持续显示场景 |
| 模板详情 | 所选模板的sceneSnapshot/topologySnapshot | 冻结初始状态或独立调试状态 | 只读总览，可放大 |
| 课件制作 | 明确绑定的模板；发布课件查看其发布快照 | 制作预览实例状态 | 场景与步骤编辑相邻 |
| 培训任务详情 | assignment.courseSnapshot | 该快照初始状态 | 开始前可看全图、目标和设备清单 |
| 学员训练 | attempt.courseSnapshot | attempt.runtime.states | 主操作区场景，支持对象选择 |
| 完成结果/归档 | 对应尝试/归档的冻结快照和事件 | 该次最终状态或选中事件的状态 | 只读，不使用最新工程草稿 |

任务列表可以使用摘要缩略图，不要求每张卡片展开全场景；任务详情和训练运行必须能看到完整图。

学员需要的场景由本人任务/尝试快照提供，不能通过向学员开放全部工程草稿来“解决图加载不到”。

第三系统应采用与教学案例匹配的设备、资源位置或工位示意，另配工序/资源关系视图；不要显示一张与当前保障案例无关的泵图。

### 2.6 R1验收

| 编号 | 操作 | 必须看到的结果 |
| --- | --- | --- |
| R1-01 | 建5对象工程，其中两对象同类，保存刷新 | 全部设备及名称、位置独立恢复 |
| R1-02 | 添加未知类型素材和边缘位置对象 | 有明确通用图形，标签完整，不出现固定泵替代 |
| R1-03 | 发布模板并用于新课件 | 工程、模板、课件显示同一份冻结布局 |
| R1-04 | 打开任务详情、进入训练 | 使用任务快照，完整场景可见并可选择对象 |
| R1-05 | 实际执行就绪、启动、拒绝和告警 | 图形状态、后端事件、结果说明一致 |
| R1-06 | 原工程修改后再看旧任务和旧结果 | 旧图和旧运行证据不随草稿变化 |
| R1-07 | 1440px、1366×768、390px检查 | 总览可看全，主动作可达，移动端能放大完整场景 |
| R1-08 | 打开旧V2课件及三系统对应入口 | 旧图兼容，新系统标题和对象正确，无统一硬编码四设备 |

交付至少提供编排、任务详情、训练运行三张实际截图及入口；截图必须来自本轮运行代码。

## 3. R2：制作、预览、运行的一致性

### 3.1 F02：草稿丢失和编辑冲突

`DeviceRelationsEditor.vue`把connections/rules保存在组件局部draft中。父组件通过`v-if`切换页签，会卸载该组件；父级dirty只跟踪对象/环境，不跟踪联动草稿。编辑联动后切换“对象与场景”或“调试预览”存在丢失路径，离开保护也覆盖不到。

另外，工程和课件保存时从最新store对象读取 `expectedEditRevision`，没有单独保留本地草稿开始编辑时的版本。发生后台刷新或冲突后的refresh时，旧草稿可能携带新的版本号再次提交，覆盖他人修改。

修改要求：

1. 由工程编辑容器持有对象、联动两个草稿，或使用以workspace/domain/projectId为键的编辑会话；统一汇总dirty及错误。
2. 页签切换保留草稿；离开工程、切换系统/身份/工作区时统一提供保存、放弃、取消。单独保留子组件并不能替代跨页面保护。
3. 明确保留 `baseEditRevision` 和原始内容。后台更新不能自动把旧草稿的基线推进到新版本。
4. 冲突时提供重新加载、保留副本或对照处理；不要自动刷新后再次覆盖。
5. 预览和发布明确使用已保存版本；有未保存修改时提示保存，不能调试旧状态却让用户以为在测试当前修改。
6. 对象重命名/删除校验连接、规则trigger/condition/effect等全部依赖；不可只检查connections。

涉及：`SimulationProjectEditor.vue`、`DeviceRelationsEditor.vue`、`CourseBuilder.vue`、`store.ts`。

### 3.2 F03：课件预览不能复用正式运行语义

本次已复现：两步课件第一步“阀门就绪”预览通过，第二步“启动泵”仍提示阀门未就绪；两次调用产生不同preview ID。`CoursewareService.preview`每次调用都执行`createPreview`，没有保留前步状态。

同时，`CourseBuilder.tryPreview`只传课件ID与stepId；后端使用课件预设parameters，用户在预览表单中输入的parameters没有传入。预览也未调用正式训练中的教学前置条件判断。

修改为“课件预览会话”：开始时冻结当前可预览版本，依次操作、继续、重试、复位和结束；按真实输入执行。抽取共用的教学步骤检查及仿真执行能力，预览不创建正式成绩或培训任务。

建议接口可采用 `coursewarePreview.start/action/continue/reset/close`，也可扩展现有接口。必须有previewId、实际target/actionId/parameters和当前步骤校验，不能只上传stepId让服务器代答。

允许单步调试，但界面必须明确它是否从初始状态运行、是否需要准备条件；不能冒充连续试学。

### 3.3 F13：异步上下文及账号角色

`store.update`当前核对workspace/epoch/revision，没有核对请求发出时的actor/domain。快速切系统或身份后，旧请求的晚到响应仍可能覆盖新页面。修复时给查询、命令、上传等统一记录上下文token/递增序号，只接收当前上下文响应；旧请求成功可保留业务结果，但不能覆盖当前视图或跳到错误记录。

`TrainingRuntimeService`、`SupportTrainingService`部分操作检查直接允许 `actor.equals("INSTRUCTOR")`；另一位角色相同的教员并不满足该条件。使用服务端角色与任务授权判断，避免将账号ID等同角色。教员查看、协助操作和最终确认的能力应清楚区分，记录真实操作者。

### 3.4 F14：显示的教学规则必须真实生效

- 课件中每步有mode，播放器主要按attempt.mode决定行为。明确“任务模式”和“步骤提示策略”的优先级；不支持的组合应限制保存/发布，不默默忽略。
- `LessonStep`选择正确对象时自动选正确动作，参数默认值取步骤答案，即使free为真仍如此。引导模式可以辅助；自由模式应使用设备默认值/空值，让学员选择，并以查看帮助作为显式行为。
- completion字段目前未形成完整验证/执行分支。首版若只支持ACTION_SUCCEEDED，后端严格限定该值并在UI说明；若增加状态条件，制作校验、预览、训练均须实现同一判定。
- 前置条件不满足时提示缺少哪个状态、如何回到允许的准备操作；不能让课件形成永远无法完成的步骤且仍通过试学验收。

### 3.5 R2验收

| 编号 | 操作 | 通过条件 |
| --- | --- | --- |
| R2-01 | 修改对象和联动，切页签再回来 | 两部分草稿都保留；离开保护覆盖全部修改 |
| R2-02 | 两窗口编辑，另一窗口保存后刷新/重试 | 旧草稿不能携带新revision覆盖远端内容 |
| R2-03 | 连续预览“阀门就绪→启动泵” | 同一实例延续状态，与正式训练结果一致 |
| R2-04 | 预览输入错误参数或未满足教学前置条件 | 使用实际输入，正确拒绝，不能由后台预设参数代答 |
| R2-05 | 制作页面离开、切工作区、切身份 | 未保存内容有明确处理，晚到数据不串页 |
| R2-06 | 自由模式及第二教员账号执行合法流程 | 不自动泄露答案；按真实角色授权并留痕 |

## 4. R3：保障教学必须由教员配置驱动

### 4.1 F04：固定六阶段与自由课件脱节

当前 `CoursewareService.createCourseware`为SUPPORT注入固定泵组案例、`Seed.operations(false)`及资源副本。教员界面仍编辑普通设备target/action步骤。

运行进入 `SupportTrainingPanel.vue` 后，固定选择E1资源、T01工序，固定六阶段与10/20/20/20/20/10评分；`SupportTrainingService`没有按发布课件的步骤列表和分值逐步驱动。

本次接口复现：发布2步、总分200的合法课件，实际仍执行六个检查点，最后得到100分。这里的问题是制作端允许的配置与运行不一致，不能靠把演示课件始终填成六步掩盖。

### 4.2 本轮目标模型

为保障教学增加明确课件类型及有限教学动作，例如：

| 教学动作类型 | 学员实际操作 | 判定依据 |
| --- | --- | --- |
| TASK_CONFIRM | 理解任务对象、目标和约束 | 确认事件及必要回答 |
| RESOURCE_CONFIGURE | 选择/编辑允许的资源 | 能力、数量、日历等案例约束 |
| PLAN_EDIT | 编辑工序、依赖和资源分配 | 引用合法、无环、必填内容完整 |
| PLAN_CALCULATE | 运行计算并判断约束 | 当前版本计算结果 |
| EVENT_HANDLE | 接收配置事件，提交处置 | 事件前后方案、参数及结果 |
| REVIEW_SUBMIT | 提交方案和复盘 | 最新结果、证据及复盘内容完整 |

六类是有限能力，不要求课件必须恰好六步。教员可以选择案例、配置允许编辑范围、事件参数、教学顺序和分值；不建设任意脚本/BPMN引擎。

案例需要可选择和修订，至少支持两个内容不同的案例。案例对象、工序、资源、时间、费用、事件在发布时冻结。保障教学中的业务任务与“分配给学员的培训任务”在界面上使用清楚的不同名称。

推荐 `lessonType=SUPPORT_PLANNING` 和 `step.kind` 等显式字段；现有旧保障课件可适配为明确的六阶段旧契约，新课件不能静默落回旧模板。

课件总分及分值规则统一：本轮可沿用默认100分；如果界面保留可编辑总分，所有运行器必须按冻结总分计算，不能某个子系统固定100。

### 4.3 F05：方案版本与结果失效

本次复现：完成加急处置得到结果后，将T01时长从30改为120，再提交复盘，服务端仍接受；存储工序是120，resolvedResult中的T01仍是30，旧结果被作为最终评价依据。

修复必须在后端实现：

1. 教学副本增加单调递增的 `planRevision`；任何影响计算的工序、资源、到货、费用修改都递增版本。
2. 计算结果记录 `inputRevision`，有必要时保存规范化输入摘要及输入快照。
3. 修改上游数据使下游结果和相关检查点失效，界面明确显示“方案已修改，请重新计算/处置”。历史结果留作历史，不能继续当当前结果。
4. 提交时校验最终结果的inputRevision与当前方案一致，事件处置及完成条件也必须对应当前版本。
5. 失效步骤的当前得分重新按有效证据计算，不能只清一个按钮状态却保留全部分数；保留历史事件。
6. 空payload不能完成资源/方案阶段；验证学员实际提交的业务内容，而不是只因接口被调用就加分。

### 4.4 R3验收

| 编号 | 操作 | 通过条件 |
| --- | --- | --- |
| R3-01 | 制作两份步骤数量或顺序不同的保障课件 | 学员显示和执行发布步骤，不固定六卡片 |
| R3-02 | 改案例、对象、资源、事件时间和分值 | 运行消费冻结配置，不能仍是E1/T01和固定时间 |
| R3-03 | 修改已算完的工序或资源 | 旧结果失效；未重算不能提交，不得取得旧结果成绩 |
| R3-04 | 两学员独立练习并回归旧案例 | 副本互不影响，正式保障预案不被修改，历史记录保留 |

## 5. R4：统一问题分流、保障任务与训练归档

### 5.1 F06：记录页登记问题失败

`views/RecordsView.vue`的`issue()`只提交 `{id, name}`。当前 `TrainingIssueService.create`要求明确的CONTENT/LEARNING/PLATFORM/EQUIPMENT_SUPPORT类型。

本次复现返回422：`VALIDATION_FAILED / 问题类型不正确`。训练播放器中已有较新表单，不能只修其中一个入口。

抽出统一问题登记表单，供训练结果、训练记录、评价页复用：选择问题类型、标题、描述，显示来源尝试和课件版本。正常训练归档仍不要求创建问题。

对旧V2记录也要明确支持策略。内容问题不能不检查课件契约就调用只接受V3的修订服务；应路由到对应兼容服务或显式转换草稿，不能报错后无下一步。

### 5.2 F07：旧入口绕过核实与业务防重

`RecordsView.issueTask`仍直接调用旧 `task.create`；`PlanningModule`接收sourceIssueId后直接创建任务，没有验证问题类型、核实状态或既有转换关系。

本次用未核实的CONTENT问题调用旧task.create，返回200并生成保障任务。新 `issue.route`正确校验，不代表整个系统已经无法绕过。

处理要求：

- 从问题发起任务，只走统一转换服务，服务端验证类型、来源状态、核实意见、角色及关联任务。
- `task.create`允许独立新建正常保障任务；但只要携带sourceIssueId，必须进入同一转换校验或明确拒绝，不能保留第二条转换路径。
- 重复转换即使命令ID不同也只返回同一关联任务；同时维护问题侧和任务侧来源关系。
- 公共记录中心改为“进入问题处理”，使用与系统内问题页相同的表单和权限，移除直接套用泵组参数的旧按钮。
- 修订、补训、技术处理、核实转任务都应能点击查看关联成果，而不只是显示内部ID。关闭问题需记录理由；不得把“已建立修订草稿”自动等同“教学问题已解决”。

### 5.3 F08：问题生成的任务无法正确承接原保障业务

当前两个任务结构并存：

| 含义 | issue.route生成 | 原PlanningModule使用 |
| --- | --- | --- |
| 任务类型 | `type` | `taskType` |
| 完成时限 | `dueMinutes` | `deadline` |
| 装备对象 | `object`自由文本 | `equipmentId/equipmentType`及相关展示 |
| 范围 | `scope` | `description/wbs`等业务内容 |

已复现：填写333分钟，任务保存dueMinutes=333；继续创建预案返回200，但预案deadline=null。不能把“生成TASK ID成功”作为转换闭环验收。

建议抽取统一的保障任务创建/校验服务，既有菜单和问题转换共同调用。此次兼容优先保留原正式业务使用的字段，必要时另存用户可读对象和范围；禁止在页面各自临时翻译。

转换表单必须明确：任务对象/类型、范围、时限及单位、约束、采用的演示任务模板或需要补充的准备内容。没有完整工序时保持“待完善”状态并引导补齐，不能偷偷套用固定泵组WBS。

历史修复：对于已生成对象可确定的dueMinutes/type，迁移到正确字段并保留来源。已经产生deadline=null的草稿预案需识别、提示并修正；无法判断的记录列为待核对，不能默认270覆盖。已归档成果不直接重算。

完整验收必须走到：问题 → 核实 → 任务 → 任务完善 → 预案创建 → 计算 → 原业务审核/演练链，确认时限、对象、类型与来源一路一致。

### 5.4 F10：归档必须独立、可读且不重复

`TrainingRuntimeService.confirm`每次生成新TRAINING-ARCHIVE，没有按attemptId检查已有归档。本次以不同commandId连续确认同一尝试，得到两条归档。

此外，`RecordsView`的training-records菜单目前主要筛选attempts；详情主要显示原始JSON，未将trainingArchives组织为用户可读成果。

修改要求：

1. 按工作区/轮次/尝试/归档类型建立业务唯一性，在现有事务内检查后创建；相同尝试重复确认返回原归档。
2. 归档确认属于具体尝试。补训不得让历史尝试的确认状态依赖assignment当前一次evidenceAttemptId而消失；任务只保存“最近尝试”摘要。
3. 归档列表区分进行中的训练、已完成待确认、已确认归档。提供按课程、学员和版本检索。
4. 详情用正常业务页面展示：课程目标、来源模板与场景、步骤结果、得分/扣分、技术异常、教员意见、反馈、归档时间和关联问题。
5. 结果场景读取该次冻结数据；JSON保留在技术详情折叠区，不能作为普通用户唯一的结果展示。
6. Markdown报告先输出可读摘要和证据表，再按需附结构化数据。报告与页面应用一致授权；学员只能访问本人允许访问的记录。
7. 已存在重复归档先识别并记录，等价内容可建立规范主记录及重复标记；不同内容需要核对，不能直接批量删除历史证据。

### 5.5 R4验收

| 编号 | 操作 | 通过条件 |
| --- | --- | --- |
| R4-01 | 从播放器、记录页分别登记四类问题 | 使用同一契约，均可保存和追溯，无缺type错误 |
| R4-02 | 用旧task.create传入未核实或内容类问题ID | 后端拒绝，不能绕过；合法核实后仅生成一个任务 |
| R4-03 | 转换333分钟任务后创建和计算预案 | 时限仍为333，类型、对象、范围和来源完整 |
| R4-04 | 两次确认同一训练，再完成补训 | 原尝试只有一个归档；补训是新证据，历史确认不丢 |
| R4-05 | 打开归档详情并导出MD | 普通用户能看懂结果；场景、事件、分数与具体尝试一致 |

## 6. R5：补齐资源、课程和台位的实际使用入口

### 6.1 F09：新台位服务没有接到现有页面

当前 `catalog.ts`仅有操作系统 `/operation/stations` 入口；`AuthoringView.vue`仍使用 `station.save/connect/signal` 老接口。新增的 `training.station.*` 接口要求实例和映射，但在现有前端未形成对应配置与执行入口。

本次还复现：

- 使用MAINTENANCE领域查询学员state，仍能看到OPERATION台位；`WorkspaceProjection`未按领域筛选stations。
- 向已完成V3尝试发送 `training.station.signal` 返回500。服务在校验结束状态之前按currentStep读取步骤，完成后下标越界产生空值。

修改要求：

1. 三系统均有“台位与外设”入口，共用组件并明确系统归属；不要求所有课件必须使用外设。
2. 新页面接 `training.station.*`，展示台位能力、连接状态、信号映射、当前目标尝试/学员及最近输入结果。
3. 任务分配可选择无需台位或指定台位；运行时按任务要求校验兼容性，不能任意同领域台位都替代指定台位。
4. 映射从明确环境/设备能力选择objectId/actionId和类型化参数；配置保存时验证，不到点击发送才发现不可用。
5. 信号处理先检查实例存在、授权、运行状态及步骤边界，再读步骤和执行；已结束、暂停、断开、未映射返回可理解的业务错误，不能500。
6. 查询、命令响应及下载中一致过滤台位范围；学员只看到训练需要的台位信息。
7. 老台位保留兼容标签，不把无领域老记录自动归为所有系统的可写台位。明确迁移映射与新旧接口边界。

### 6.2 F11：素材能力与联动规则仍偏向预置示例

`SimulationCapabilities.enrichAsset`根据componentId是否包含PUMP/VALVE/SENSOR/CTRL配置能力，其余统一CONFIRM/RESET。新素材表单不提供对应能力配置，默认GENERIC；“上传自己的模型”不等于得到可用于新业务步骤的设备能力。

`DeviceRelationsEditor`可增加连接，但规则主要通过“生成泵组联动示例”建立；现有规则只能修改部分条件值和拒绝文案，缺少通用触发对象、动作、条件/效果的完整编辑入口。

本轮采用有限、可解释的能力配置：

- 素材导入后选择模拟能力模板、视觉符号及用途；显示它支持哪些动作、输入输出、状态字段。文档/图片/视频不要自动当成可操纵设备。
- 工程中能配置声明过的初始状态，按BOOLEAN/NUMBER/ENUM等展示表单；目前只编辑initialState.status不足以配置传感器初值等。
- 联动规则支持新增、复制、删除，选择触发对象/动作、条件对象/字段/运算符/值以及结果对象/字段/值；使用现有类型化规则引擎。
- 中文预览完整解释“什么时候、满足什么条件、改变什么、失败如何提示”；泵组示例保留为可选模板，不是唯一规则创建方法。
- 发布快照冻结能力与视觉描述。以后平台能力升级不能无提示覆盖历史课件动作定义。

不在本轮建设任意代码脚本或真实设备驱动。有限能力模板可以先覆盖识别、参数输入、状态动作、检查记录等演示需求。

### 6.3 F12：课程组织和任务参数需要可见操作

当前有curricula集合及增加课时的后端能力，但新课件弹窗主要要求重新填写课程组织/课时；缺少选择已有课程课时、查看课程结构和维护多个课件的完整界面。

任务分配弹窗目前只选择学员，后端支持的mode、dueAt、stationId没有对应完整表单。用户难以理解“课程、课时、课件、任务”的关系，也无法通过页面完整设置任务。

补齐最低能力：

| 对象 | 必要操作 | 用户看到的关系 |
| --- | --- | --- |
| 课程 | 新建、目标/受众维护、查看课时 | 一门课程包含若干课时 |
| 课时 | 新增、排序、选择关联课件 | 本课时使用哪些发布版本 |
| 课件 | 从课程课时创建，选择/修订版本 | 环境、步骤、评分与发布状态 |
| 培训任务 | 选发布版本、学员、模式、期限、可选台位 | 学员应完成什么、何时完成、在哪里操作 |
| 学员进度 | 未开始、进行中、待确认、已归档、补训关系 | 当前状态、下一步、已有结果 |

不要求新增复杂教务系统。优先在当前系统工作台和课件页面中补清楚这些入口；批量分配可简单生成多条单人任务。

业务页面不要要求教员理解“V2/V3契约”“P5已开放”“不会落入旧训练器”等内部实现词。默认提供正常制作流程，旧课件兼容放到明确的高级/兼容入口。

### 6.4 R5验收

| 编号 | 操作 | 通过条件 |
| --- | --- | --- |
| R5-01 | 三系统分别配置台位并给指定尝试发信号 | 入口完整、映射有效、跨域和实例权限隔离 |
| R5-02 | 完成后/暂停时/未连接时发送信号 | 返回业务说明，不报500，不改状态和成绩 |
| R5-03 | 新导入素材配置能力与初始值 | 教员可从页面完成，不依赖手工调用API或改JSON |
| R5-04 | 不用泵组示例创建另一套合法规则 | 能新增条件/效果并调试，保存刷新可恢复 |
| R5-05 | 同课程建两个课时与不同课件，再分配任务 | 可选已有组织；期限、模式、台位进入任务快照/配置 |

## 7. R6：按普通用户操作重新验收

### 7.1 F15：现有验证为何没有发现这些问题

`verify-p7-three-routes.mjs`使用HTTP请求组织三条路线，本身不启动浏览器；操作/维修课件中的多步训练统一使用CONFIRM。它可以验证接口基本连通，但无法证明教员能够从页面完成同样制作，也无法充分验证设备状态前后依赖。

仓库另有浏览器专项，这些应保留。但 `verify-p7-delivery.mjs`中“截图文件存在”和“文档中写了通过”只能做交付完整性检查，不能作为业务正确或视觉清晰的独立证据。

新验收分三层，分别报告：

1. 服务层：规则、状态机、版本、授权、业务防重及错误路径。
2. 页面操作层：使用实际菜单、表单和按钮完成业务，不用API替代被验收的核心页面步骤。
3. 演示理解层：读页面能知道当前任务、操作位置、成功/失败原因和下一步；查看完整设备图和实际结果。

测试可通过API准备登录、资源夹具等前置条件，但必须注明哪些业务步骤真正经页面完成。

### 7.2 三条实际演示路线

| 路线 | 最低场景 | 必须通过页面完成 |
| --- | --- | --- |
| 装备使用及作战运用 | 5个对象，含两台同类设备、一个前置条件和一个模拟告警 | 编排与联动、模板发布、课件制作、任务分配、实际状态操作、评价归档 |
| 装备维修保障 | 设备、部件、工具和检查对象；可无跨设备联动 | 工程和课件制作、工具/对象选择、检查记录、错误纠正、评价与补训 |
| 保障任务筹划及行为演练 | 两个不同教学案例，至少一个修改后需要重算 | 案例选择、步骤配置、资源/工序修改、计算、事件处置、复盘和归档 |

设备操作可以全部模拟，业务输入、条件、变化和保存必须真实。不要再次以连续点击同一个确认动作代替全部演示内容。

### 7.3 R6验收

| 编号 | 检查 | 通过条件 |
| --- | --- | --- |
| R6-01 | 新用户按手册完成三条路线 | 不需演示者补充隐藏步骤，不需手写API |
| R6-02 | 场景与事件一致性 | 新对象、版本、运行状态及结果图一致；原V2路线保留 |
| R6-03 | 问题转任务至原保障预案/演练 | 校验和字段统一，不能只验证创建成功 |
| R6-04 | 构造旧schema=7数据迁移并重启 | 迁移幂等，旧课程/成绩/证据不被重写，需核对项明确 |
| R6-05 | 同次构建同步根目录运行包并冷启动 | 用户从start.bat/start.sh看到本轮成果，原数据库保持 |

### 7.4 回归命令与执行顺序

在仓库根进入source，按环境使用npm/mvn或对应Windows命令：

```bash
cd source
npm --prefix frontend ci
npm --prefix frontend run build
mvn -B -ntp -f backend/pom.xml verify
node scripts/verify-browser.mjs
node scripts/verify-training-workflow.mjs
node scripts/verify-three-systems.mjs
```

按修改范围执行已有P2—P7专项，并添加本轮针对性用例；不要求每个小修改重复跑全部脚本。最终交付前运行完整约定回归。

```bash
node scripts/sync-release-package.mjs
node scripts/verify-release-package.mjs
node scripts/verify-p7-delivery.mjs
```

先构建及测试，再同步和冷启动。`verify-p7-delivery`对报告/截图的存在性检查可保留，但应增加生成时间/本次构建标识和真实业务断言的关联；不能为了通过而直接改Markdown验收状态。

保留原操作/维修评分、原保障排程费用、三级归档、领域隔离和历史快照等已有金标，不修改旧预期来掩盖失败。新增案例按其真实输入计算，不混用不同到货费用输入的结果。

## 8. 分批交付顺序和修改地图

### 8.1 分批执行，逐批展示成果

| 批次 | 交付目标 | 用户应立即看到的成果 |
| --- | --- | --- |
| R1 | 统一完整设备场景 | 编排、任务详情、训练中的同一套完整设备图 |
| R2 | 草稿与预览/训练一致 | 联动草稿不丢，连续试学与正式操作一致 |
| R3 | 保障教学配置与结果版本 | 两份不同课件真实驱动，改方案必须重算 |
| R4 | 问题、保障任务和归档契约 | 所有登记入口可用，时限正确传递，归档不重复 |
| R5 | 资源能力、组织和台位入口 | 教员能从页面配置完整业务，无需补API |
| R6 | 三系统人工验收及运行包 | 新用户可按手册独立演示，源码和启动包一致 |

R3/R4中的结果正确性阻断项可以在完成R1后优先修复；具体每轮范围写清楚。禁止自动把一次任务扩展为全部批次，也不要在R1完成后声称所有业务问题已经解决。

### 8.2 主要修改位置

| 模块 | 关键现有文件 | 重点 |
| --- | --- | --- |
| 场景表现 | SceneView.vue、LessonStep.vue、SimulationProjectEditor.vue、style.css | 统一动态设备渲染、边界适配、状态反馈 |
| 场景接入 | SimulationPreviewPanel.vue、SimulationTemplatesView.vue、CourseBuilder.vue、TrainingAssignments.vue、TrainingPlayer.vue | 正确快照与实例来源 |
| 草稿/上下文 | DeviceRelationsEditor.vue、CourseBuilder.vue、store.ts、App.vue | dirty汇总、固定编辑基线、晚到响应隔离 |
| 教学预览/运行 | CoursewareService.java、TrainingRuntimeService.java、SimulationRuleEngine.java | 共用判定、预览会话、真实参数、模式语义 |
| 保障教学 | SupportTrainingPanel.vue、SupportTrainingService.java、CoursewareService.java | 有限教学动作、案例配置、结果版本失效 |
| 问题/正式任务 | RecordsView.vue、TrainingIssues.vue、TrainingIssueService.java、PlanningModule.java | 统一契约、唯一转换、任务字段规范化 |
| 训练归档 | TrainingRuntimeService.java、RecordsView.vue、TrainingView.vue、DemoController.java、DemoService.java | 业务防重、历史确认、可读详情/报告 |
| 台位 | catalog.ts、AuthoringView.vue、TrainingRuntimeService.java、TrainingDomain.java、WorkspaceProjection.java | 三系统入口、实例信号、权限与状态检查 |
| 素材/能力 | AssetsView.vue、SimulationCapabilities.java、SimulationProjectService.java | 能力/视觉模板、类型化初始值、历史冻结 |
| 迁移/验证 | WorkspaceMigration.java、Seed.java、source/scripts/、后端测试 | 从当前schema=7增量接续，保护历史和运行包 |

表中Vue/TS文件位于 `source/frontend/src/` 对应components或views目录；Java文件位于 `source/backend/src/main/java/cn/peixun/demo/`。执行前核对当前分支实际位置，建议新增文件不需要机械照抄名称。

### 8.3 数据保护与兼容

- 当前 `WorkspaceMigration.CURRENT_SCHEMA_VERSION=7`。有持久化结构变更时新增7→8或后续迁移，不能改写原1→7步骤的含义。
- 视觉元数据、教学动作类型、计划版本和任务规范化按需引入；旧课件契约、旧模板、完成记录继续可读。
- 先在独立测试库和构造夹具验证。真实用户数据库不作为测试夹具；如需实际迁移，先做一致备份，记录版本、数量与待核对问题。
- 不删除旧全局配置来迫使所有旧课程升级；新业务不得重新依赖它。保持明确兼容边界。
- 服务端对旧命令访问新对象也要限制或适配，不能只修前端分流；特别检查course.*、station.*、task.create等旧入口。
- 保持现有任务、尝试、模板快照和领域隔离；不为补页面直接开放全部后台集合。

### 8.4 每批必须交付的Markdown记录

更新 `source/docs/业务重构实施进度.md`、`本轮验收记录.md`、`三系统演示操作手册.md`；有迁移时更新 `数据迁移与兼容说明.md`。

每批报告包括：起始SHA、修复的F编号、修改文件、实际入口、操作步骤、对应R验收结果、测试命令、运行包状态、尚未修复的问题。部分完成就写部分完成，不沿用上一轮“全部完成”结论。

## 9. 本次实际复现记录

以下均在最新源码编译的独立测试服务中执行，未使用或修改用户演示数据库。

| 复现 | 输入/操作 | 实际结果 | 对应问题 |
| --- | --- | --- | --- |
| 两步预览 | 阀门SET_READY=true，再预览泵START | 第一步通过；第二步“阀门未就绪”；两次不同preview ID | F03 |
| 重复归档 | 同一完成尝试，两次training.confirm，commandId不同 | trainingArchives中同attemptId有2条 | F10 |
| 记录页登记契约 | issue.create仅提交id/name | HTTP 422：问题类型不正确 | F06 |
| 新转换任务 | 已核实问题，dueMinutes=333，再plan.create | task.dueMinutes=333，plan.deadline=null | F08 |
| 旧转换路径 | CONTENT未核实问题，task.create传sourceIssueId | HTTP 200并生成任务 | F07 |
| 完成后台位输入 | 向COMPLETED尝试发送映射信号 | HTTP 500：INTERNAL_ERROR | F09 |
| 保障课件与运行 | 发布2步/200分课件，执行保障教学 | 固定六检查点，最终100分 | F04 |
| 过期计算结果 | 处置完成后T01从30改120再提交 | 接受提交；operations为120，resolvedResult仍为30 | F05 |
| 台位领域投影 | OPERATION建台位，学员查询MAINTENANCE | 返回OPERATION台位记录 | F09 |

前端构建通过及接口可调用，并不能消除这些问题。本轮没有把缺失浏览器内核导致的未执行检查写成通过。

## 10. 固定提交证据入口

- [最新审阅提交88eeb96](https://github.com/lizhao013216-prog/peixun/commit/88eeb967f5d0441b6d4d71c4561ab67eba577d47)
- [阶段实施报告](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/docs/业务重构实施进度.md)
- [新旧场景分支LessonStep](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/frontend/src/components/LessonStep.vue)
- [工程编辑工作区](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/frontend/src/views/SimulationProjectEditor.vue)
- [课件预览与发布服务](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/backend/src/main/java/cn/peixun/demo/CoursewareService.java)
- [保障教学服务](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/backend/src/main/java/cn/peixun/demo/SupportTrainingService.java)
- [训练运行、台位与归档](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/backend/src/main/java/cn/peixun/demo/TrainingRuntimeService.java)
- [记录页旧入口](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/frontend/src/views/RecordsView.vue)
- [问题分流服务](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/backend/src/main/java/cn/peixun/demo/TrainingIssueService.java)
- [正式任务与预案服务](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/backend/src/main/java/cn/peixun/demo/PlanningModule.java)
- [三路线HTTP验证脚本](https://github.com/lizhao013216-prog/peixun/blob/88eeb967f5d0441b6d4d71c4561ab67eba577d47/source/scripts/verify-p7-three-routes.mjs)

后续仓库可能继续更新。Codex应先判断以上问题是否仍存在，再做对应增量修改；不能将本文件的旧基线覆盖到用户当前分支。
