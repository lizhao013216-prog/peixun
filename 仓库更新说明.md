# Demo 1.2 仓库结构说明

## 当前结构

仓库根目录同时保存可直接启动的运行包和完整源码：

| 路径 | 用途 |
| --- | --- |
| `start.bat`、`start.sh` | 启动根目录运行包 |
| `backend/` | 可运行JAR和直接启动版本的数据目录 |
| `frontend/dist/` | 直接启动版本使用的前端静态文件 |
| `source/` | 前端、后端、脚本和独立开发配置的完整源码 |
| `docs/` | 交付说明与验证记录 |
| `.github/workflows/ci.yml` | 从 `source/` 执行构建和自动验证 |

原有的外层 `peixun-demo-v1.0/` 包装目录已移除。运行包与源码的相对关系保持不变，没有把运行产物和源码目录混合。

## 直接运行

需要Java 17。Windows双击仓库根目录的 `start.bat`；Linux/macOS执行：

```bash
bash start.sh
```

随后访问 http://127.0.0.1:8080 。启动脚本会进入根目录的 `backend/`，JAR从 `../frontend/dist/` 提供页面，因此目录上移后无需修改脚本。

运行数据位于 `backend/.data/`。移动和备份该目录前应先停止程序，避免数据库正在写入。

## 源码开发与验证

完整源码位于 `source/`。构建与验证命令以该目录为工程根目录，具体方法见 `source/README.md` 和 `docs/开发与运行说明.md`。

根CI已经使用以下路径：

- `source/frontend/package-lock.json`
- `source/frontend/`
- `source/backend/pom.xml`
- `source/scripts/`

运行版 `frontend/dist/` 明确纳入版本管理，以保证全新克隆后也能直接启动；`source/frontend/dist/`、`node_modules/`、`target/`、测试结果和新生成的数据文件继续忽略。

## 已验证范围

- 前端TypeScript校验和生产构建。
- 后端Maven测试。
- 页面路由和完整培训业务浏览器验证。
- 根目录JAR启动、健康接口、首页及页面引用的JS/CSS资源。

业务实现范围和未完成事项见 `docs/实现范围与验证记录.md`。
